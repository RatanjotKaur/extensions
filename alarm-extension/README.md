# React + Vite + TypeScript Chrome Extension (Alarm)


1. npm install
2. npm run build
3. Go to chrome://extensions -> Load unpacked -> choose dist/


Notes:
- Make sure you use Node 22.x or compatible (Vite requirement).
- If you use NVM on Windows, uninstall global Node or ensure nvm-managed node is active.